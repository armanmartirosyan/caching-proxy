// import logger from "./helpers/logger";

// logger.debug(env);

// process.on("uncaughtException", (e) => {
// });

// process.on("unhandledRejection", (e) => {
// });

// process.on("exit", () => {
//   process.exit(1);
// });
